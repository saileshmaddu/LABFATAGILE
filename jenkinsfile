stage('Push Image') {
    steps {
        script {
            docker.withRegistry('https://docker.io', 'docker-hub-credentials-id') {
                dockerImage.push("latest")
            }
        }
    }
}
